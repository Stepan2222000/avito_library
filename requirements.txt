# Зависимости боевого кода. Версии зафиксированы по тем, на которых всё замерялось.
#
# curl_cffi — единственный клиент, чей отпечаток TLS Авито пропускает: httpx и pycurl
#   на тех же адресах получают блокировку, это мерялось отдельно.
# opencv и numpy нужны только решателю капчи: он ищет вырез в картинке.
# pycryptodome в Debian ставится как Cryptodome, поэтому решатель пробует оба имени.

curl_cffi==0.15.0
asyncpg==0.31.0
opencv-python-headless==4.13.0.92
numpy>=2.3
pycryptodome==3.23.0
